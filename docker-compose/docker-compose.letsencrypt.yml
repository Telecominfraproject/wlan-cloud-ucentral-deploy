version: '3'

services:
  ucentralgw:
    env_file:
      - .env.letsencrypt

  ucentralsec:
    env_file:
      - .env.letsencrypt

  ucentralfms:
    env_file:
      - .env.letsencrypt

  traefik:
    volumes:
      - "./traefik/ucentral_letsencrypt.yaml:/etc/traefik/ucentral.yaml"
